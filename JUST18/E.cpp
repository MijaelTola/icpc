#include <bits/stdc++.h>

using namespace std;

int t,n;
int v[20][20];

bool ver(int bit, int bit2) {
    for (int i = 0; i < n; ++i) {
    
    }
}
int f(int pos, int p, int bitmask) {
    if(pos == n) return 0;
    int ans = 0;

    for (int i = p; i < n; ++i) {
    
    }

}
//01001000
//00000010
int main() {
    scanf("%d",&t);
    while(t--) {
        scanf("%d",&n);
        for (int i = 0; i < n; ++i) 
            for (int j = 0; j < n; ++j)
                scanf("%d", &v[i][j]);
        
    }
    return 0;
}

