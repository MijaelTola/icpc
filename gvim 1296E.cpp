#include <bits/stdc++.h>

using namespace std;

int ans[200010];
vector<int> v[27];

int main() {
    memset(ans, 1, sizeof ans);   

    int n;
    strings;
    cin >> n >> s;
    
    for (int i = 0; i < (int)s.size(); ++i) 
        v[s[i] - 'a'].push_back(i);
    
    return 0;
}

