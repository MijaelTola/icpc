#include <bits/stdc++.h>

using namespace std;

int n;
long long a[100010];
long long b[100010];
vector<int> G[100010];

int main() {

    cin >> n;

    for (int i = 1; i <= n; ++i)
        cin >> a[i];

    for (int i = 1; i <= n; ++i)
        cin >> b[i];

    for (int i = 0; i < n - 1; ++i) {
        int x,y;
        cin >> x >> y;
    return 0;
}

