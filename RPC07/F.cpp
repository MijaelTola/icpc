#include <iostream>
#include <algorithm>
#include <queue>
#include <stack>
#include <vector>
#include <climits>
#include <map>
#include <set>
#include <cassert>
#include <cstdio>
#include <cstring>

using namespace std;

typedef long long ll;

ll l;
int a,b,c,d;
ll m[5][5100];
int dp[5][100000000];
int v[5];

int main(){
    
    while(cin >> l){
        memset(dp,-1,sizeof dp);
        for (int i = 0; i < 4; ++i) cin >> v[i];0];
int v[5];
int f(int row, int column){
    if(row == 4) return 0;

}
int main(){
 
        if(!l) return 0;
        for (int i = 0; i < 4; ++i)
            for (int j = 0; j < v[i]; ++j) cin >> m[i][j];
            cout << f(0,0,0) << endl;
    }


    return 0;
}

