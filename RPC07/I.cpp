#include <bits/stdc++.h>

using namespace std;

int solve(string s){
	vector<int> v;
	
	for (int i = 0; i < s.size(); ++i){
		
	}
}
string c[3];
int main(){
	
	int n,m; cin >> n >> m;
	cin.ignore();
	string s;
	for (int i = 0; i < n; ++i) 
		getline(cin,c[i]);
	
	for (int i = 0; i < n; ++i){
		for (int j  = 0; j < m; ++j){
			cout << c[i][j];
		}
	}
	return 0;
}
