#include <bits/stdc++.h>

using namespace std;

int v[100010];
char s[300010];
int main() {
    int k,n;
    scanf("%d %d", &k, &n);
    int last = -1;
    for (int i = 0; i < n; ++i)
        scanf("%d", v + i);

    scanf("%s", s);
    int sz = strlen(s);

    for (int i = 0; i < sz; ++i) {
    
    }
    return 0;
}

