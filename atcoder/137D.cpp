#include <bits/stdc++.h>

using namespace std;

int n,m;
vector<pair<int,int> > v;

int bs(int l) {
    int a = -1, b = n;

    while(b - a > 1) {
        int mid = (a + b) / 2;

    }
}
int f(int pos) {

    int left = m - pos;
    

}
int main() {
    cin >> n >> m;

    for (int i = 0; i < n; ++i) {
        int x,y;
        cin >> x >> y;
        v.emplace_back(x,y);
    }

    sort(v.begin(), v.end());

    return 0;
}

